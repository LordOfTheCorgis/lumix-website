---
import Layout from "../layouts/Layout.astro";
import { leadership, teams, siteConfig } from "../data/site";
---

<Layout title="Our Team" description="Meet the team behind Lumix Solutions. Our leadership and dedicated teams work around the clock to deliver reliable infrastructure.">
  <!-- Page Header -->
  <section>
    <div>
      <div>
        <span>
          <span>$</span> cat /team/README.md
        </span>
        <h1>
          Our<br/>
          <span>team</span>
        </h1>
        <p>
          Meet the people behind Lumix Solutions. A dedicated team of engineers 
          and specialists committed to delivering reliable, high-performance 
          infrastructure.
        </p>
      </div>
    </div>
  </section>
  
  <!-- Leadership Section -->
  <section>
    <div>
      <span>// LEADERSHIP</span>
      <h2>
        Executive Team
      </h2>
      
      <div>
        {leadership.map((member, index) => (
          <div>
            <div>
              <div>
                <img 
                  src={member.image} 
                  alt={member.name}
                  loading="lazy"
                  onerror="this.onerror=null;this.src='/images/logo.png';"
                />
                <div></div>
              </div>
              <span>
                {member.title.split(' ').map(w => w[0]).join('')}
              </span>
              <h3>
                {member.name}
              </h3>
              <p>
                {member.title}
              </p>
              <p>
                {member.description}
              </p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>
  
  <div></div>
  
  <!-- Teams Section -->
  <section>
    <div>
      <span>// TEAMS</span>
      <h2>
        Our Teams
      </h2>
      <p>
        Behind every service is a dedicated team working around the clock to 
        ensure your infrastructure stays online and performs at its best.
      </p>
      
      <div>
        {teams.map((team, index) => (
          <div>
            <div>
              <div>
                {team.icon === "code" && (
                  <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
                  </svg>
                )}
                {team.icon === "headset" && (
                  <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 11a7 7 0 01-7 7m0 0a7 7 0 01-7-7m7 7v4m0 0H8m4 0h4m-4-8a3 3 0 01-3-3V5a3 3 0 116 0v6a3 3 0 01-3 3z" />
                  </svg>
                )}
                {team.icon === "shield" && (
                  <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
                  </svg>
                )}
                {team.icon === "server" && (
                  <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 12h14M5 12a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v4a2 2 0 01-2 2M5 12a2 2 0 00-2 2v4a2 2 0 002 2h14a2 2 0 002-2v-4a2 2 0 00-2-2m-2-4h.01M17 16h.01" />
                  </svg>
                )}
              </div>
              <div>
                <h3>
                  {team.name}
                </h3>
                <p>
                  {team.description}
                </p>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>
  
  <div></div>
  
  <!-- Join Us CTA -->
  <section>
    <div>
      <div>
        <span>// JOIN_US</span>
        <h2>
          Want to join our team?
        </h2>
        <p>
          We're always looking for talented individuals who are passionate about 
          infrastructure and networking. Get in touch if you'd like to be part of 
          the Lumix team.
        </p>
        <a href="/contact">
          Get in Touch
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
          </svg>
        </a>
      </div>
    </div>
  </section>
</Layout>

